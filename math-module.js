// const stringModule = require("./strings-module.js");

const sum = (a,b) => a + b;
const multiply = (a,b) => a * b;
const divide = (a, b) => a / b;
const square = (a) => a**2;

module.exports = {
  sum,
  multiply,
  divide,
  square
}
